worker_processes  1;
events { worker_connections 1024; }
http {
  server {
    listen 80;
    server_name localhost;
    root /website;
    location / {
      gzip_static on;
      try_files $uri $uri/ =404;
    }
  }
}
